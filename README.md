LeetCode SQL 50

Completed the SQL 50 study plan — 50/50 ✅

Concepts Covered


Joins (inner, left, self-join, cross join)
Aggregate functions (COUNT, SUM, AVG, MAX, MIN)
GROUP BY / HAVING
CASE WHEN for conditional logic and bucketing
Subqueries (scalar, correlated)
IN, EXISTS, NOT EXISTS
UNION to combine result sets
Window functions (ROW_NUMBER, RANK, LAG, LEAD, running totals with SUM() OVER)
Date functions (DATEDIFF, BETWEEN)
String functions (CONCAT, SUBSTRING, LIKE, REGEXP)
Handling NULLs and default values (IFNULL)
